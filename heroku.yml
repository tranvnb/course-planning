setup:
  config:
    DATABASE_URI: mongodb+srv://<username>:<password>@<database-host>/<database>?retryWrites=true
build:
  docker:
    web: Dockerfile
run:
  web: sed -i -e 's/$PORT'"$PORT"'/g' /etc/nginx/conf.d/default/default.conf && nginx -g 'daemon off;'
